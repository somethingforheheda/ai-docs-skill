---
name: ai-docs
description: 管理项目的 ai-docs 任务文档系统，实现跨会话任务持续性。触发场景：新建任务文档、更新任务进度、归档已完成任务、恢复或继续 current/xxx 任务、列出当前任务。用户说开个任务、记一下、记录进度、更新进度、继续 xxx、接着做、归档等均适用；即使没提 ai-docs，只要意图是记录正在做的事、恢复上次的工作或归档完成的需求，都应主动使用。
---

# ai-docs 任务文档 Skill

ai-docs 是一套让 AI 跨会话持续工作的任务文档管理方案。每个任务是一个目录，正文按用途拆成多个文件，实时记录进度和下一步，新会话只需读文档即可无缝接续。

---

## 核心能力

| 能力 | 说明 |
|------|------|
| 新建任务文档 | 在 `ai-docs/current/` 下创建任务目录，写入索引与分文件 |
| 更新任务进度 | 把新进展、变更文件、技术决策写入对应分文件 |
| 归档任务 | 补完成总结，目录移入 `ai-docs/archive/` |
| 恢复任务上下文 | 读索引与 `todo.md`，从「下一步」继续执行 |
| 查看当前任务 | 列出 `ai-docs/current/` 下各任务及状态 |

---

## 触发条件

| 用户说的 | 执行操作 |
|---------|---------|
| 新建任务 / 开始记录 / 开个任务 / 记一下 | 新建任务文档 |
| 更新进度 / 记录进度 | 更新任务进度 |
| 归档 / 任务完成 | 归档任务 |
| 继续 / 恢复 / 接着做 | 恢复任务上下文 |
| 列出当前任务 | 查看当前任务 |

即使用户没有明确提到 ai-docs，只要意图是记录正在做的事、恢复上次的工作或归档完成的需求，都应主动使用本 skill。

---

## 工作流程

```
┌─────────────────────┐
│ 第一步 确认工作目录 │
└──────────┬──────────┘
           │
           ▼
   ai-docs/ 是否存在？
           │
      ┌────┴─────┐
      │否        │是
      ▼          │
┌──────────────┐ │
│ 第二步 初始化 │ │
└──────┬───────┘ │
       └────┬────┘
            ▼
┌───────────────────────┐
│ 第三步 操作类型判断    │
└──────────┬────────────┘
           │
   ┌───────┼───────┬───────┬───────┐
   ▼       ▼       ▼       ▼       ▼
 新建    更新    归档    恢复    查看
```

### 第一步：确认工作目录

每次触发都先逐条执行以下命令（各自独立调用，不要串联）：

```bash
pwd
```

```bash
ls -d .git
```

```bash
ls -d ai-docs
```

后两条命令返回非零退出码即表示对应目录不存在。

将 `pwd` 结果告知用户：「我将在 [路径] 下操作。」

如果不是 git 根目录（无 `.git/`），提醒用户确认是否继续。

### 第二步：初始化（仅 ai-docs 不存在时）

**ai-docs 已存在 → 直接进入第三步。**

ai-docs 不存在时，先征求用户同意：
> 「当前项目没有 `ai-docs/`，将创建 `current/` `archive/` `templates/` 三个目录并写入任务模板，是否继续？」

**关于 git 忽略**：在运行脚本前单独询问用户：
> 「是否将 `ai-docs/` 添加到 `.git/info/exclude`（仅本机生效，不影响 `.gitignore`）？」

用户同意后运行初始化脚本（脚本路径与本 SKILL.md 同级，即 `~/.claude/skills/ai-docs/scripts/init.sh`）。

仅创建目录与模板：

```bash
bash ~/.claude/skills/ai-docs/scripts/init.sh
```

同时添加 git 本地忽略（用户明确同意时才加此参数）：

```bash
bash ~/.claude/skills/ai-docs/scripts/init.sh --with-git-exclude
```

### 第三步：操作类型判断

按上文「触发条件」表格对应到具体操作，跳转到下面对应小节执行。

---

## 任务目录结构（分文件）

每个任务是一个目录，正文按用途拆成多个文件，而不是塞进一个大 md：

```
ai-docs/current/{任务名}/
├── {任务名}.md       # 索引摘要：基本信息 + 任务目标 + 各文件链接 + 一句话现状
├── requirement.md    # 需求详情：背景、需求描述、字段/接口定义、待确认事项
├── todo.md           # 进度追踪：已完成/进行中/待完成/下一步
├── decisions.md      # 关键决策：问题/决策/理由
├── changes.md        # 变更文件清单：新增文件/修改文件
└── resources/        # 配套资源：图片、Excel、drawio 等，非 md 内容一律放这里
```

**{任务名}.md 只做索引和摘要，不写细节**：细节内容分别写进对应的分文件。新会话恢复任务时先读 {任务名}.md 定位现状，需要细节再读对应分文件。

对已存在、仍是单文件旧格式（`{任务名}.md` 里塞了所有内容，没有 `todo.md`/`requirement.md` 等分文件）的任务：**只兼容读取，不自动迁移**。读取时直接在这个单文件里按章节查找对应信息即可；只有用户明确要求「拆分」「迁移」这个旧任务时，才动手拆。

---

## 使用示例

### 示例 1：新建任务文档

1. 确认任务名称（未提供则从对话推断）
2. 用 bash 获取当前真实时间（**禁止用模型内部记忆的日期**）：
   ```bash
   date +"%Y-%m-%d %H:%M"
   ```
3. 创建任务目录（含 resources 子目录）：
   ```bash
   mkdir -p "ai-docs/current/[任务名]/resources"
   ```
4. 参照 `ai-docs/templates/` 下的各模板，用 Write 工具写入以下文件，将对话中已知的信息填入：
   - `[任务名].md` —— 索引摘要（基本信息、任务目标、各分文件链接、当前状态一句话摘要），创建时间填 `date` 命令输出的值
   - `requirement.md` —— 需求背景与详情（如果对话中已有明确需求内容）
   - `todo.md` —— 进度追踪
   - `decisions.md` —— 关键决策（暂无可留模板占位）
   - `changes.md` —— 变更文件清单（暂无可留模板占位）
   - 如对话中有图片/截图/导出文件需要保留，复制到 `resources/` 下，用有意义的文件名（不用时间戳或哈希），并在 requirement.md 或 {任务名}.md 里引用路径
5. 告知用户文档路径

**文档路径格式**：`ai-docs/current/{任务名}/{任务名}.md`（索引文件必须以任务名命名）

### 示例 2：更新任务进度

1. 找到对应任务目录（`ai-docs/current/` 下）；如有多个，先问用户更新哪个
2. 判断是分文件结构还是旧单文件结构（看是否存在 `todo.md`）
3. 根据对话中发生的事情更新对应文件：

| 发生了什么 | 分文件结构 → 更新位置 | 旧单文件结构 → 更新位置 |
|-----------|---------|---------|
| 完成了一个步骤 | `todo.md` → 已完成（`[ ]` 改 `[x]`） | 同一文件「进度追踪→已完成」 |
| 在做某件事 | `todo.md` → 进行中 | 同一文件「进度追踪→进行中」 |
| 下一步计划 | `todo.md` → 下一步（要具体可执行） | 同一文件「进度追踪→下一步」 |
| 新增了文件 | `changes.md` → 新增文件 | 同一文件「变更文件清单→新增文件」 |
| 修改了文件 | `changes.md` → 修改文件（标注 `file:line`） | 同一文件「变更文件清单→修改文件」 |
| 做了重要技术决策 | `decisions.md` | 同一文件「关键决策」 |
| 需求内容有补充/变化 | `requirement.md` | 同一文件对应章节 |
| 新增图片/资源文件 | 复制到 `resources/`，在引用处加相对路径链接 | 同上 |

4. 分文件结构下，若 `{任务名}.md` 里的「当前状态摘要」已过时，同步更新这一句话

### 示例 3：归档任务

1. 分文件结构：在 `{任务名}.md` 追加「完成总结」小节（实现概述、经验教训），或作为单独 `summary.md` 写入（二者选一，若已有类似章节直接补充即可）
   旧单文件结构：在「完成总结」章节填写实现概述和经验教训
2. 移动目录：
   ```bash
   mv "ai-docs/current/[任务名]" "ai-docs/archive/[任务名]"
   ```
3. 告知用户归档路径

### 示例 4：恢复任务上下文

1. 先读 `ai-docs/current/[任务名]/[任务名].md`（索引摘要），了解基本信息和现状
2. 根据摘要判断是否需要读分文件获取细节（一般至少读 `todo.md` 确认下一步；涉及需求细节读 `requirement.md`；涉及历史技术选择读 `decisions.md`）
3. 汇报：已完成了什么、当前进行中的步骤、下一步要做什么
4. 直接从「下一步」描述的动作开始执行

### 示例 5：查看当前任务

```bash
ls ai-docs/current/
```

如有多个任务，读取各自 `{任务名}.md` 索引摘要（或旧结构的「进度追踪」部分），简要说明各任务状态。

---

## 错误处理

| 情况 | 处理方式 |
|------|---------|
| 当前目录不是 git 根目录 | 告知用户当前路径，询问是否仍在此创建 ai-docs |
| `ai-docs/` 不存在 | 先征求同意再运行 init.sh，不要静默创建 |
| `current/` 下有多个同名或相似任务 | 列出候选，让用户选择，不要猜 |
| 指定的任务目录不存在 | 列出 `current/` 现有任务，询问是否新建 |
| 任务是旧单文件格式 | 在该文件内按章节读写，不自动迁移 |
| `init.sh` 执行失败 | 脚本已 `set -e`，读取报错输出定位原因；模板文件已存在时会跳过而非覆盖 |
| 无法获取当前时间 | 必须重试 `date` 命令，禁止用模型记忆中的日期填写 |

---

## 目录结构

```
ai-docs/
├── current/                  # 进行中的任务
│   └── {需求名}/
│       ├── {需求名}.md       # 索引摘要（必须有，用任务名命名，只做索引不写细节）
│       ├── requirement.md    # 需求详情：背景、描述、字段/接口定义、待确认事项
│       ├── todo.md           # 进度追踪：已完成/进行中/待完成/下一步
│       ├── decisions.md      # 关键决策：问题/决策/理由
│       ├── changes.md        # 变更文件清单：新增/修改文件
│       └── resources/        # 配套资源：图片、Excel、drawio、导出文件等
├── archive/                  # 已完成的任务（结构与 current/ 相同）
└── templates/
    ├── task-template.md          # 索引摘要模板
    ├── requirement-template.md   # 需求详情模板
    ├── todo-template.md          # 进度追踪模板
    ├── decisions-template.md     # 关键决策模板
    └── changes-template.md       # 变更清单模板
```

**兼容旧格式**：早期版本里任务是单个 `{任务名}.md` 塞所有内容。遇到这种任务目录（无 `todo.md` 等分文件）时，直接在该文件内按章节读写，不强制迁移；用户主动要求时才按上面的分文件结构拆分。

---

## 代码规范

- 所有代码示例必须从实际工程中复制，确保 100% 真实性
- 推测内容标注「[推测]」或「[基于架构推断]」
- 代码位置标注 `file_path:line_number`
