---
name: ai-docs
description: |
  管理项目的 ai-docs 任务文档系统，实现跨会话任务持续性。
  当用户说以下任何一种时，必须使用这个 skill：
  - "记录到ai-docs" / "记录任务" / "写到ai-docs"
  - "新建任务文档" / "创建任务" / "开个任务"
  - "更新任务进度" / "更新进度" / "记录进度"
  - "归档任务" / "任务完成，归档" / "归档 xxx"
  - "继续 current/xxx 任务" / "接着做 xxx" / "恢复任务"
  - 用户完成一个功能后说"记一下" / "记录一下"
  - 任何涉及任务跟踪、进度记录、跨会话恢复的场景
  即使用户没有明确说 ai-docs，只要他们想记录正在做的事情、恢复上次的工作、归档完成的需求，都应主动使用这个 skill。
---

# ai-docs 任务文档 Skill

ai-docs 是一套让 AI 跨会话持续工作的任务文档管理方案。每个任务有一个文档，实时记录进度和下一步，新会话只需读文档即可无缝接续。

---

## 第一步：确认工作目录

每次触发都先执行：

```bash
pwd
ls .git 2>/dev/null && echo "is git root" || echo "not git root"
ls ai-docs/ 2>/dev/null && echo "ai-docs exists" || echo "ai-docs missing"
```

将 `pwd` 结果告知用户："我将在 `<路径>` 下操作。"

如果不是 git 根目录（无 `.git/`），提醒用户确认是否继续。

---

## 第二步：初始化（仅 ai-docs 不存在时）

**ai-docs 已存在 → 直接跳到"操作类型判断"。**

ai-docs 不存在时，先征求用户同意：
> "当前项目没有 `ai-docs/`，将创建 `current/` `archive/` `templates/` 三个目录并写入任务模板，是否继续？"

用户同意后运行初始化脚本（脚本路径与本 SKILL.md 同级）：

```bash
# 仅创建目录+模板
bash <skill-path>/scripts/init.sh

# 如需同时加 git 本地忽略（需单独询问用户）
bash <skill-path>/scripts/init.sh --with-git-exclude
```

> `<skill-path>` 为本 skill 所在目录，即 `~/.claude/skills/ai-docs`。

**关于 git 忽略**：在运行脚本前单独询问用户：
> "是否将 `ai-docs/` 添加到 `.git/info/exclude`（仅本机生效，不影响 `.gitignore`）？"

根据回答决定是否加 `--with-git-exclude` 参数。

---

## 操作类型判断

| 用户说的 | 执行操作 |
|---------|---------|
| 新建任务 / 开始记录 | → 新建任务文档 |
| 更新进度 / 记录进度 | → 更新任务进度 |
| 归档 / 任务完成 | → 归档任务 |
| 继续 / 恢复 / 接着做 | → 恢复任务上下文 |
| 列出当前任务 | → 查看当前任务 |

---

## 新建任务文档

1. 确认任务名称（未提供则从对话推断）
2. 用 bash 获取当前真实时间（**禁止用模型内部记忆的日期**）：
   ```bash
   date +"%Y-%m-%d %H:%M"
   ```
3. 创建任务目录：
   ```bash
   mkdir -p "ai-docs/current/<任务名>"
   ```
4. 用 Write 工具写入 `ai-docs/current/<任务名>/<任务名>.md`，参照 `ai-docs/templates/task-template.md` 的结构，将对话中已知的信息填入（目标、已完成步骤、变更文件等），创建时间填 `date` 命令输出的值
5. 告知用户文档路径

**文档路径格式**：`ai-docs/current/{任务名}/{任务名}.md`（主文档必须以任务名命名）

---

## 更新任务进度

1. 找到对应任务文档（`ai-docs/current/` 下）；如有多个，先问用户更新哪个
2. 读取当前内容
3. 根据对话中发生的事情更新：

| 发生了什么 | 更新位置 |
|-----------|---------|
| 完成了一个步骤 | 进度追踪 → 已完成（`[ ]` 改 `[x]`） |
| 在做某件事 | 进度追踪 → 进行中 |
| 下一步计划 | 进度追踪 → 下一步（要具体可执行） |
| 新增了文件 | 变更文件清单 → 新增文件 |
| 修改了文件 | 变更文件清单 → 修改文件（标注 `file:line`） |
| 做了重要技术决策 | 关键决策 |

---

## 归档任务

1. 读取任务文档，在"完成总结"章节填写实现概述和经验教训
2. 移动目录：
   ```bash
   mv "ai-docs/current/<任务名>" "ai-docs/archive/<任务名>"
   ```
3. 告知用户归档路径

---

## 恢复任务上下文

1. 读取 `ai-docs/current/<任务名>/<任务名>.md`
2. 汇报：已完成了什么、当前进行中的步骤、下一步要做什么
3. 直接从"下一步"描述的动作开始执行

---

## 查看当前任务

```bash
ls ai-docs/current/
```

如有多个任务，读取各自文档的"进度追踪"部分，简要说明各任务状态。

---

## 目录结构

```
ai-docs/
├── current/              # 进行中的任务
│   └── {需求名}/
│       ├── {需求名}.md   # 主文档（必须有，用任务名命名）
│       └── *.drawio / *.html / *.md  # 可选：配套图表、分析等
├── archive/              # 已完成的任务
└── templates/
    └── task-template.md  # 任务模板（由 init.sh 生成）
```

---

## 代码规范

- 所有代码示例必须从实际工程中复制，确保 100% 真实性
- 推测内容标注"[推测]"或"[基于架构推断]"
- 代码位置标注 `file_path:line_number`
