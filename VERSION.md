# 版本记录

## v1.2.0 — 2026-07-28

- SKILL.md 结构对齐规范：新增「核心能力」「触发条件」「工作流程」「使用示例」「错误处理」标准章节
- 新增 ASCII 工作流程图
- 拆分第一步的复合命令，改为单条独立调用（不再使用 `&&` / `||` 串联）
- `description` 改为单行字符串（原为多行 `|` 块标量），精简至 176 字符
- 正文引号统一为中文书名号「」，避免 YAML 与校验器误判
- 新增本 VERSION.md

## v1.1.0 — 2026-07-22

- 任务文档从单个大 md 拆分为分文件结构：`{任务名}.md` 只做索引摘要，正文分入 `requirement.md` / `todo.md` / `decisions.md` / `changes.md`
- 每个任务新增 `resources/` 目录，存放图片、Excel、drawio 等非 md 资源
- `init.sh` 由生成 1 个模板改为生成 5 个模板
- 兼容旧单文件格式：只读取不自动迁移，用户明确要求时才拆分
- README.md 同步分文件目录结构说明

## v1.0.0 — 2026-07-22

- 首个版本：ai-docs 跨会话任务文档管理 skill
- 支持新建任务、更新进度、归档任务、恢复上下文、查看当前任务五类操作
- `scripts/init.sh` 初始化 `current/` `archive/` `templates/` 目录结构
- 可选将 `ai-docs/` 写入 `.git/info/exclude`
